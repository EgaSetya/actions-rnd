git_url("https://github.com/tanookiai/bythen-ios-credentials.git")
git_basic_authorization(ENV["MATCH_GIT_BASIC_AUTHORIZATION"])
storage_mode("git")

type("development") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier([
    "ai.bythen.bythenapp",
    "ai.bythen.bythenapp.dev"
])

# When using CI, this should be one centralized iOS team email
# For now, you can it to your apple id email
username "egasp57@gmail.com"
